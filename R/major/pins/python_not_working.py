import pins
from pins import board_local, board_folder, board_temp, board_urls

from pins import mtcars

board = pins.board_temp()

import pins.

board.pin_search("cars")

# TEMP_PATH = '.'
# board <- board_folder(TEMP_PATH)
# board %>% pin_read("mtcars")
